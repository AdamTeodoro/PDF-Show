{
  "name": "pdfshow",
  "version": "1.0.0",
  "description": "reader and writer for pdf",
  "main": "build/index.js",
  "scripts": {
    "start": "npm run compile && electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc && copyfiles -f src/index.html build"
  },
  "author": "Adam Teodoro",
  "license": "MIT",
  "dependencies": {
    "electron": "^11.1.0",
    "typescript": "^4.1.3"
  }
}
